#include "utilidades.h"
#include <stdio.h>

void RegistrarError(const char *mensajeError) {
    // Función simple para mostrar mensajes de error en la consola.
    printf("Error: %s\n", mensajeError);
}
